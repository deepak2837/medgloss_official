import React from 'react';

const SearchByYear = () => {
    return (
        <div>
            <h2>Search by Year</h2>
            {/* Add content for searching by year */}
        </div>
    );
};

export default SearchByYear;
